<!DOCTYPE html>
<html>

<head>
    <title></title>
</head>

<body>
    <%- include ('../layout') %>
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h3 class="my-4" align="center">จัดการข้อมูลห้องพัก</h3>
                <a href="/admins/addChamber" class="mx-1 btn btn-info">เพิ่มห้องพัก</a>
                <a href="/" class="mx-1 btn btn-info">ดูหน้าห้องพัก</a>
                <a href="/admins/history" class="mx-1 btn btn-info">ประวัติการจองห้องพัก</a>
                <br>
                <br>
                <br>
                <table class="table">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">ชื่อห้องพัก</th>
                            <th scope="col">วันที่อัพเดท</th>
                            <th scope="col">Edit</th>
                            <th scope="col">Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% chambers.forEach(function(chamber){ %>
                        <tr>
                            <th scope="row"><%= chamber.name %></th>
                            <td><%= chamber.date %></td>
                            <td><a href="/admins/editChamber/<%= chamber._id %>" class="btn btn-dark">Edit</a></td>
                            <td>
                                <form action="/admins/deleteChamber/" method="POST" class="delete-chamber">
                                    <input type="hidden" name="id" value="<%=chamber._id %>">
                                    <input type="submit" class="btn btn-secondary" value="Delete">
                                </form>
                            </td>
                        </tr>
                        <%})%>
                    </tbody>
                </table>
            </div>

            <div class="col-md-6">
                <h3 class="my-4" align="center">จัดการข้อมูลโฆษณา</h3>
                <a href="/admins/addAdvertise" class="mx-1 btn btn-info">เพิ่มโฆษณา</a>
                <a href="/members/chambers" class="mx-1 btn btn-info">ดูหน้าโฆษณา</a>
                <br>
                <br>
                <br>
                <table class="table">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">โฆษณา</th>
                            <th scope="col">วันที่อัพเดท</th>
                            <th scope="col">Edit</th>
                            <th scope="col">Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% advertises.forEach(function(advertise){ %>
                        <tr>
                            <th scope="row"><%= advertise.name %></th>
                            <td><%= advertise.date %></td>
                            <td><a href="/admins/editAdvertise/<%= advertise._id %>" class="btn btn-dark">Edit</a></td>
                            <td>
                                <form action="/admins/deleteAdvertise/" method="POST" class="delete-advertise">
                                    <input type="hidden" name="id" value="<%=advertise._id %>">
                                    <input type="submit" class="btn btn-secondary" value="Delete">
                                </form>
                            </td>
                        </tr>
                        <%})%>
                    </tbody>
                </table>
            </div>

        </div>
    </div>
</body>
<script type="text/javascript">
    $(document).ready(function () {
        $('.delete-chamber').on('submit', function () {
            if (confirm("คุณต้องการลบข้อมูลหรือไม่")) {
                return true
            } else {
                return false
            }
        })
    })
    $(document).ready(function () {
        $('.delete-advertise').on('submit', function () {
            if (confirm("คุณต้องการลบข้อมูลหรือไม่")) {
                return true
            } else {
                return false
            }
        })
    })
</script>

</html>